spring:
  datasource:
    url: jdbc:h2:mem:test_mem
    username:
    password:
    driver-class-name: org.h2.Driver
  jpa:
    properties:
      hibernate:
        hbm2ddl:auto: update

db:
  username: test
  password: test
